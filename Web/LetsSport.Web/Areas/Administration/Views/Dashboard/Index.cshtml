@model LetsSport.Web.ViewModels.Administration.Dashboard.IndexViewModel
@{
    this.ViewData["Title"] = "Admin dashboard";
}

<h2 class="display-4">@this.ViewData["Title"]</h2>
<hr />

<div class="align-items-md-center">
    <label class="h4" asp-for="@Model.UsersCount"></label>
    <span class="h3">@Model.UsersCount</span>
</div>
<div class="align-items-md-center">
    <label class="h4" asp-for="@Model.CountriesCount"></label>
    <span class="h3">@Model.CountriesCount</span>
</div>
<div class="align-items-md-center">
    <label class="h4" asp-for="@Model.CitiesCount"></label>
    <span class="h3">@Model.CitiesCount</span>
</div>
<div class="align-items-md-center">
    <label class="h4" asp-for="@Model.SportsCount"></label>
    <span class="h3">@Model.SportsCount</span>
</div>
<div class="align-items-md-center">
    <label class="h4" asp-for="@Model.EventsCount"></label>
    <span class="h3">@Model.EventsCount</span>
</div>
<div class="align-items-md-center">
    <label class="h4" asp-for="@Model.ArenasCount"></label>
    <span class="h3">@Model.ArenasCount</span>
</div>
<div class="align-items-md-center">
    <label class="h4" asp-for="@Model.ReportsCount"></label>
    <span class="h3">@Model.ReportsCount</span>
</div>
<div class="align-items-md-center">
    <label class="h4" asp-for="@Model.MessagesCount"></label>
    <span class="h3">@Model.MessagesCount</span>
</div>
